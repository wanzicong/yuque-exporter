[银行流水匹配优化-智领平台-TAPD平台](https://www.tapd.cn/55357004/prong/stories/view/1155357004001026298)

数据过慢问题:

现状: 

1. 查询数据 多条数据
2. 更新
3. 删除
4. 推送消息
    1. 消费消息
    2. 重新查一边
    3. 更新状态
    4. 处理核心业务



核心 core -> 简单操作批量， 复杂操作并发 ， 和查询相关的并发， 和 添加删除相关的批量

优化建议:

1. 查询数据 多条数据，过滤，符合条件-》 分批 100 一批 ， 支持配置化
2. 设置业务字段
3. 批量更新 保证事务
4. 批量删除 保证事务
5. 推送批量消息 （推送批量消息id ， 推送前设置uuid ，保证消费幂等，因为批量消费可能会超时）
    1.  获取批量id ， 保持原有的业务处理逻辑
    2. 使用线程池并发操作
    3. 优化里面的查询慢sql
        1. 单表查询;<font style="color:#080808;background-color:#ffffff;">bill_collection_map 大表+索引 done getWaitMoney</font>

```plain
bankFlowVerifyTaskService.getCustomerMainList
  - ActionResult<List<FinanceCustomerResp>> result = customerApi.getCustomerMainInfoList(financeCustomerReq);
  lease 服务
    - contract_bank_info 表-记得加索引
    - customer_contact 表
    - customer_company 表
  
bankFlowVerifyTaskService.getBillList
- order_bill
 单表查询， 涉及排序，排序字段加索引，根据条件加索引
```

        2. <font style="color:#080808;background-color:#ffffff;">updateBankFlowWait 最多的操作， 建议并发查询数据，符合条件的操作 缓存起来，批量操作【jnpf.base.service.finance.helper.BankFlowV2Helper#dealData】</font>
        3. <font style="color:#080808;background-color:#ffffff;">dealBillMapData 最多的操作 建议将符合条件的数据， 汇总并发</font>
        4. <font style="color:#080808;background-color:#ffffff;">两个最细力度的操作，方法 </font><font style="color:#DF2A3F;background-color:#ffffff;">updateBankFlowWait   dealBillMapData</font>





<font style="color:#117CEE;">总结:</font>

1. <font style="color:#117CEE;">并发查询，计算，if if if ..... 获取到需要添加 删除 修改的 实体</font>
2. <font style="color:#117CEE;">放入缓存</font>
3. <font style="color:#117CEE;">在并发外部，批量 删除， 更新， 添加 </font>
4. <font style="color:#117CEE;">easy easy easy>>>>>>> 干就完了， 没有连表操作</font>



风险还好，并发操作 只有查询，事务可以保证在一批消息中

